import 'package:flutter/material.dart';

class ZwanenhalsPage extends StatelessWidget {
  const ZwanenhalsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Zwanenhals')),
      body: Container(),
    );
  }
}
